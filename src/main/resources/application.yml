spring:
  security:
    user:
      name: root
      password: root
  datasource:
    username: root
    password: root
    url: jdbc:mysql:///demo?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai